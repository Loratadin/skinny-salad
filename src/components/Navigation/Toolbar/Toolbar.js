import React from 'react';
import { Toolbar } from './Toolbar.style';

const toolbar = (props) => (
    <Toolbar>
        <div>MENU</div>
        <div>LOGO</div>
        <nav>
            ...
        </nav>
    </Toolbar>
);

export default toolbar;